// Code generated by sdk-factory (scripts/generate.sh); DO NOT EDIT.

package loginradius

import (
	"net/http"
	"time"
)

// defaultHTTPClient constructs the http.Client used when the caller does not
// supply one via WithHTTPClient. We deliberately set conservative defaults
// suited to a server-side SDK: a short request timeout and the standard
// transport (which already pools connections).
func defaultHTTPClient(timeout time.Duration) *http.Client {
	return &http.Client{
		Timeout:   timeout,
		Transport: http.DefaultTransport,
	}
}

// wrapTransport installs the SDK's authTransport on top of whatever Transport
// the caller's http.Client uses. The original Transport is preserved as the
// base, so any custom dialer/proxy/TLS config the caller put together still
// runs.
func wrapTransport(client *http.Client, cfg *config) {
	client.Transport = newAuthTransport(cfg, client.Transport)
}

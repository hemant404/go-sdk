<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# JWT Custom Providers

5 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `CreateJwtSPClientConfiguration`

**POST** `/v2/manage/custom-providers/jwt`

Create JWT SP configuration

Creates a new Service Provider (SP) configuration for a JWT client in the Tenant, defining details such as endpoints, mapping, and other required settings.

### Example

```go
func callCreateJwtSPClientConfiguration(ctx context.Context, client *Client) {
  createJwtSPClientConfigurationRequest := NewCreateJwtSPClientConfigurationRequest("<appName>", "<algo>", "<mapping>") //Required

  resp, _, err := client.JWTCustomProviders.CreateJwtSPClientConfiguration(ctx).CreateJwtSPClientConfigurationRequest(*createJwtSPClientConfigurationRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`object` as `application/json`.

### Returns

`JwtSpConfig`

---

## `DeleteJwtSPClientConfigurationByAppName`

**DELETE** `/v2/manage/custom-providers/jwt/{jwtApp}`

Delete JWT SP configuration

Deletes the Service Provider (SP) configuration for a JWT client in the Tenant identified by the AppName, permanently disabling the application's service provider integration.

### Example

```go
func callDeleteJwtSPClientConfigurationByAppName(ctx context.Context, client *Client) {
  jwtApp := "<jwtApp>" //Required

  resp, _, err := client.JWTCustomProviders.DeleteJwtSPClientConfigurationByAppName(ctx, jwtApp).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `jwtApp` | path | string | yes | The jwt App identifier |

### Returns

`DeleteResponse`

---

## `GetAllJwtConfigSPConfigurations`

**GET** `/v2/manage/custom-providers/jwt`

List JWT SP configurations

Retrieves a list of all Service Provider (SP) configurations associated with JWT clients for the Tenant, including endpoints, mapping, and other settings for each SP setup.

### Example

```go
func callGetAllJwtConfigSPConfigurations(ctx context.Context, client *Client) {
  resp, _, err := client.JWTCustomProviders.GetAllJwtConfigSPConfigurations(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `GetJwtSPClientConfigurationByAppName`

**GET** `/v2/manage/custom-providers/jwt/{jwtApp}`

Retrieve JWT SP configuration

Retrieves the Service Provider (SP) configuration details for a JWT client in the Tenant using the AppName, including endpoints, mapping, and other configured settings.

### Example

```go
func callGetJwtSPClientConfigurationByAppName(ctx context.Context, client *Client) {
  jwtApp := "<jwtApp>" //Required

  resp, _, err := client.JWTCustomProviders.GetJwtSPClientConfigurationByAppName(ctx, jwtApp).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `jwtApp` | path | string | yes | The jwt App identifier |

### Returns

`JwtSpConfig`

---

## `UpdateJwtSPClientConfigurationByAppName`

**PUT** `/v2/manage/custom-providers/jwt/{jwtApp}`

Update JWT SP configuration

Updates an existing Service Provider (SP) configuration for a JWT client in the Tenant identified by the AppName, modifying settings such as endpoints, mapping, or other configuration details.

### Example

```go
func callUpdateJwtSPClientConfigurationByAppName(ctx context.Context, client *Client) {
  jwtApp := "<jwtApp>" //Required
  jwtSpConfigBaseModel := JwtSpConfigBaseModel{} //Required
  jwtSpConfigBaseModel.SetAlgo("<algo>")
  jwtSpConfigBaseModel.SetMapping("<mapping>")
  jwtSpConfigBaseModel.SetKey("<key>")

  resp, _, err := client.JWTCustomProviders.UpdateJwtSPClientConfigurationByAppName(ctx, jwtApp).JwtSpConfigBaseModel(jwtSpConfigBaseModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `jwtApp` | path | string | yes | The jwt App identifier |

### Request body

`JwtSpConfigBaseModel` as `application/json`.

### Returns

`JwtSpConfig`


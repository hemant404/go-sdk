<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Organization User Roles

6 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AssignRolesToUser`

**PUT** `/v2/manage/account/{uid}/orgcontext/{orgId}/roles`

Assign Roles in Organization

Assigns Roles to a User within a specific Organization.

### Example

```go
func callAssignRolesToUser(ctx context.Context, client *Client) {
  uid := "<uid>" //Required
  orgId := "<orgId>" //Required
  userRolePutRequest := NewUserRolePutRequest("<roleIds>") //Required

  resp, _, err := client.OrganizationUserRoles.AssignRolesToUser(ctx, uid, orgId).UserRolePutRequest(*userRolePutRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | path | string | yes | UID of the User |
| `orgId` | path | string | yes | Organization ID |

### Request body

`UserRolePutRequest` as `application/json`.

### Returns

`object`

---

## `AssignRolesToUserInAllOrgs`

**PUT** `/v2/manage/account/{uid}/orgcontext/roles`

Assign Roles in Tenant

Assigns Roles to a User within a Tenant.

### Example

```go
func callAssignRolesToUserInAllOrgs(ctx context.Context, client *Client) {
  uid := "<uid>" //Required

  resp, _, err := client.OrganizationUserRoles.AssignRolesToUserInAllOrgs(ctx, uid).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | path | string | yes | UID of the User |

### Returns

`object`

---

## `DeleteOrgContextByUid`

**DELETE** `/v2/manage/account/{uid}/orgcontext`

Delete Organization context by UID

Deletes User Roles for all Organizations by UID.

### Example

```go
func callDeleteOrgContextByUid(ctx context.Context, client *Client) {
  uid := "<uid>" //Required

  resp, _, err := client.OrganizationUserRoles.DeleteOrgContextByUid(ctx, uid).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | path | string | yes | UID of the User |

### Returns

`DeleteResponse`

---

## `DeleteOrgContextByUidAndOrgId`

**DELETE** `/v2/manage/account/{uid}/orgcontext/{orgId}`

Delete Organization Roles by OrgID and UID

Deletes User Roles of an Organization by UID and OrgID.

### Example

```go
func callDeleteOrgContextByUidAndOrgId(ctx context.Context, client *Client) {
  uid := "<uid>" //Required
  orgId := "<orgId>" //Required

  resp, _, err := client.OrganizationUserRoles.DeleteOrgContextByUidAndOrgId(ctx, uid, orgId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | path | string | yes | UID of the User |
| `orgId` | path | string | yes | Organization ID |

### Returns

`DeleteResponse`

---

## `GetOrgContextByUid`

**GET** `/v2/manage/account/{uid}/orgcontext`

Retrieve Organization context by UID

Retrieves User Roles for all Organizations by UID.

### Example

```go
func callGetOrgContextByUid(ctx context.Context, client *Client) {
  uid := "<uid>" //Required

  resp, _, err := client.OrganizationUserRoles.GetOrgContextByUid(ctx, uid).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | path | string | yes | UID of the User |

### Returns

`object`

---

## `GetOrgContextByUidAndOrgId`

**GET** `/v2/manage/account/{uid}/orgcontext/{orgId}`

Retrieve Organization Roles by OrgID and UID

Retrieves User Roles of an Organization by UID and OrgID.

### Example

```go
func callGetOrgContextByUidAndOrgId(ctx context.Context, client *Client) {
  uid := "<uid>" //Required
  orgId := "<orgId>" //Required

  resp, _, err := client.OrganizationUserRoles.GetOrgContextByUidAndOrgId(ctx, uid, orgId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `uid` | path | string | yes | UID of the User |
| `orgId` | path | string | yes | Organization ID |

### Returns

`object`


<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Captcha Configuration

2 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `GetCaptchaConfiguration`

**GET** `/v2/manage/captcha`

Retrieve captcha configuration

Retrieves the captcha configuration settings for a specific Tenant.

### Example

```go
func callGetCaptchaConfiguration(ctx context.Context, client *Client) {
  resp, _, err := client.CaptchaConfiguration.GetCaptchaConfiguration(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`CaptchaConfig`

---

## `UpdateCaptchaConfiguration`

**PUT** `/v2/manage/captcha`

Update captcha configuration

Updates the captcha configuration settings for a specific Tenant.

### Example

```go
func callUpdateCaptchaConfiguration(ctx context.Context, client *Client) {
  captchaConfig := CaptchaConfig{} //Required
  captchaConfig.SetIsEnabled(true)
  captchaConfig.SetQQTencentCaptcha("<qQTencentCaptcha>")
  captchaConfig.SetGoogleRecaptchaV2("<googleRecaptchaV2>")

  resp, _, err := client.CaptchaConfiguration.UpdateCaptchaConfiguration(ctx).CaptchaConfig(captchaConfig).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`CaptchaConfig` as `application/json`.

### Returns

`CaptchaConfig`


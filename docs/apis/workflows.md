<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Workflows

8 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AddWorkflow`

**POST** `/v2/manage/workflows`

Add workflow

Adds a new workflow configuration to the Tenant.

### Example

```go
func callAddWorkflow(ctx context.Context, client *Client) {
  addWorkflowConfig := NewAddWorkflowConfig("<name>", "<data>") //Required

  resp, _, err := client.Workflows.AddWorkflow(ctx).AddWorkflowConfig(*addWorkflowConfig).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`AddWorkflowConfig` as `application/json`.

### Returns

`WorkflowConfig`

---

## `DeleteWorkflow`

**DELETE** `/v2/manage/workflows/{workflowId}`

Delete Workflow

Deletes an existing Workflow from the system.

### Example

```go
func callDeleteWorkflow(ctx context.Context, client *Client) {
  workflowId := "<workflowId>" //Required

  resp, _, err := client.Workflows.DeleteWorkflow(ctx, workflowId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |

### Returns

`DeleteResponse`

---

## `DeleteWorkflowVersion`

**DELETE** `/v2/manage/workflows/{workflowId}/versions/{version}`

Delete Workflow Version

Deletes a specific version of a Workflow from the system.

### Example

```go
func callDeleteWorkflowVersion(ctx context.Context, client *Client) {
  workflowId := "<workflowId>" //Required
  version := "<version>" //Required

  resp, _, err := client.Workflows.DeleteWorkflowVersion(ctx, workflowId, version).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |
| `version` | path | string | yes | The version identifier to delete. |

### Returns

`IsDeleted`

---

## `GetAllWorkflows`

**GET** `/v2/manage/workflows`

List workflows

Retrieves a list of all workflows configured for the Tenant.

### Example

```go
func callGetAllWorkflows(ctx context.Context, client *Client) {
  resp, _, err := client.Workflows.GetAllWorkflows(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `GetAllWorkflowVersionList`

**GET** `/v2/manage/workflows/{workflowId}/versions`

List Workflow Versions

Returns a list of all available versions for a specified Workflow.

### Example

```go
func callGetAllWorkflowVersionList(ctx context.Context, client *Client) {
  workflowId := "<workflowId>" //Required

  resp, _, err := client.Workflows.GetAllWorkflowVersionList(ctx, workflowId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |

### Returns

`VersionListResponse`

---

## `GetWorkflowById`

**GET** `/v2/manage/workflows/{workflowId}`

Retrieve Workflow

Retrieves details of a specific Workflow using its unique identifier.

### Example

```go
func callGetWorkflowById(ctx context.Context, client *Client) {
  workflowId := "<workflowId>" //Required

  resp, _, err := client.Workflows.GetWorkflowById(ctx, workflowId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |

### Returns

`WorkflowConfig`

---

## `RestoreWorkflowVersion`

**PUT** `/v2/manage/workflows/{workflowId}/versions/{version}`

Restore Workflow Version

Restores a specific version of a Workflow to its active state.

### Example

```go
func callRestoreWorkflowVersion(ctx context.Context, client *Client) {
  workflowId := "<workflowId>" //Required
  version := "<version>" //Required

  resp, _, err := client.Workflows.RestoreWorkflowVersion(ctx, workflowId, version).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |
| `version` | path | string | yes | The version identifier to delete. |

### Returns

`object`

---

## `UpdateWorkflow`

**PUT** `/v2/manage/workflows/{workflowId}`

Update Workflow

Updates the configuration of an existing Workflow.

### Example

```go
func callUpdateWorkflow(ctx context.Context, client *Client) {
  workflowId := "<workflowId>" //Required
  updateWorkflowConfig := UpdateWorkflowConfig{} //Required
  updateWorkflowConfig.SetName("<name>")
  updateWorkflowConfig.SetThemeName("<themeName>")
  updateWorkflowConfig.SetDescription("<description>")

  resp, _, err := client.Workflows.UpdateWorkflow(ctx, workflowId).UpdateWorkflowConfig(updateWorkflowConfig).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `workflowId` | path | string | yes | The ID of the workflow. |

### Request body

`UpdateWorkflowConfig` as `application/json`.

### Returns

`WorkflowConfig`


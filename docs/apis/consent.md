<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Consent

7 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AddConsentForm`

**POST** `/consent/forms`

Add Consent Form

Adds a new Consent Form for the Tenant.

### Example

```go
func callAddConsentForm(ctx context.Context, client *Client) {
  consentFormModel := ConsentFormModel{} //Required
  consentFormModel.SetEvents("<events>")
  consentFormModel.SetStartFromDate("<startFromDate>")
  consentFormModel.SetConsentOptions("<consentOptions>")

  resp, _, err := client.Consent.AddConsentForm(ctx).ConsentFormModel(consentFormModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`ConsentFormModel` as `application/json`.

### Returns

`ConsentForm`

---

## `CreateConsentOption`

**POST** `/consent/options`

Create Consent Option

Creates a new consent option for a specific Tenant.

### Example

```go
func callCreateConsentOption(ctx context.Context, client *Client) {
  consentOptionModel := ConsentOptionModel{} //Required
  consentOptionModel.SetTitle("<title>")
  consentOptionModel.SetDescription("<description>")

  resp, _, err := client.Consent.CreateConsentOption(ctx).ConsentOptionModel(consentOptionModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`ConsentOptionModel` as `application/json`.

### Returns

`ConsentOptions`

---

## `DeleteConsentForm`

**DELETE** `/consent/forms/{version}`

Delete Consent Form

Deletes the Consent Form identified by the form version for the Tenant.

### Example

```go
func callDeleteConsentForm(ctx context.Context, client *Client) {
  version := "<version>" //Required

  resp, _, err := client.Consent.DeleteConsentForm(ctx, version).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `version` | path | string | yes | The version of the Consent form to delete. |

### Returns

`IsDeleted`

---

## `DeleteConsentOption`

**DELETE** `/consent/options/{optionId}`

Delete Consent Option

Deletes the consent option identified by the option ID for the Tenant.

### Example

```go
func callDeleteConsentOption(ctx context.Context, client *Client) {
  optionId := "<optionId>" //Required

  resp, _, err := client.Consent.DeleteConsentOption(ctx, optionId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `optionId` | path | string | yes | The ID of the Consent option to delete. |

### Returns

`IsDeleted`

---

## `GetActiveConsentForms`

**GET** `/consent/forms/active`

Retrieve Active Consent Forms

Retrieves a list of active Consent Forms configured for the Tenant.

### Example

```go
func callGetActiveConsentForms(ctx context.Context, client *Client) {
  event := "<event>" //Required

  resp, _, err := client.Consent.GetActiveConsentForms(ctx).Event(event).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `event` | query | string | yes | Event type to filter consent verification (e.g., `login`). |

### Returns

`object`

---

## `GetConsentForms`

**GET** `/consent/forms`

Retrieve Consent Forms

Retrieves all Consent Forms configured for the Tenant.

### Example

```go
func callGetConsentForms(ctx context.Context, client *Client) {
  resp, _, err := client.Consent.GetConsentForms(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `GetConsentOptions`

**GET** `/consent/options`

Retrieve Consent Options

Lists all consent options available for a specific Tenant.

### Example

```go
func callGetConsentOptions(ctx context.Context, client *Client) {
  resp, _, err := client.Consent.GetConsentOptions(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`


<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# SAML Custom Providers

7 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `CreateSAMLSPClientConfiguration`

**POST** `/v2/manage/custom-providers/saml`

Create SAML SP Configuration

Creates a new Service Provider configuration for a SAML client within the Tenant, defining necessary settings for SAML authentication flows.

### Example

```go
func callCreateSAMLSPClientConfiguration(ctx context.Context, client *Client) {
  samlSpConfigModel := NewSamlSpConfigModel("<provider>", "<dataMap>") //Required

  resp, _, err := client.SAMLCustomProviders.CreateSAMLSPClientConfiguration(ctx).SamlSpConfigModel(*samlSpConfigModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`SamlSpConfigModel` as `application/json`.

### Returns

`SamlSpConfig`

---

## `DeleteSAMLSPClientConfigurationByAppName`

**DELETE** `/v2/manage/custom-providers/saml/{samlApp}`

Delete SAML SP Configuration

Deletes the Service Provider configuration for a SAML client within the Tenant, identified by the application name.

### Example

```go
func callDeleteSAMLSPClientConfigurationByAppName(ctx context.Context, client *Client) {
  samlApp := "<samlApp>" //Required

  resp, _, err := client.SAMLCustomProviders.DeleteSAMLSPClientConfigurationByAppName(ctx, samlApp).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `samlApp` | path | string | yes | The SAML App identifier |

### Returns

`DeleteResponse`

---

## `GetAllSAMLSPClientConfigurations`

**GET** `/v2/manage/custom-providers/saml`

List SAML SP Configurations

Retrieves a list of all Service Provider configurations for SAML clients within the Tenant, including details such as datamap, endpoints, and certificates.

### Example

```go
func callGetAllSAMLSPClientConfigurations(ctx context.Context, client *Client) {
  resp, _, err := client.SAMLCustomProviders.GetAllSAMLSPClientConfigurations(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `GetSAMLSPClientConfigurationByAppName`

**GET** `/v2/manage/custom-providers/saml/{samlApp}`

Retrieve SAML SP Configuration

Retrieves the Service Provider configuration details for a SAML client within the Tenant, identified by the application name.

### Example

```go
func callGetSAMLSPClientConfigurationByAppName(ctx context.Context, client *Client) {
  samlApp := "<samlApp>" //Required

  resp, _, err := client.SAMLCustomProviders.GetSAMLSPClientConfigurationByAppName(ctx, samlApp).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `samlApp` | path | string | yes | The SAML App identifier |

### Returns

`SamlSpConfig`

---

## `GetSamlSPClientMappingKeys`

**GET** `/v2/manage/custom-providers/saml/keys`

Retrieve SAML SP Mapping Keys

Retrieves a list of mapping keys available for configuring attribute mappings in SAML Service Provider clients within the Tenant.

### Example

```go
func callGetSamlSPClientMappingKeys(ctx context.Context, client *Client) {
  resp, _, err := client.SAMLCustomProviders.GetSamlSPClientMappingKeys(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `RenewSAMLSppCertificate`

**POST** `/v2/manage/custom-providers/saml/{samlApp}/renew-certificate`

Renew SAML SP Certificate

Renews the SAML Service Provider certificate to replace an expiring or compromised certificate.

### Example

```go
func callRenewSAMLSppCertificate(ctx context.Context, client *Client) {
  samlApp := "<samlApp>" //Required

  resp, _, err := client.SAMLCustomProviders.RenewSAMLSppCertificate(ctx, samlApp).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `samlApp` | path | string | yes | The SAML App identifier |

### Returns

`SamlSpConfig`

---

## `UpdateSAMLSPClientConfigurationByAppName`

**PUT** `/v2/manage/custom-providers/saml/{samlApp}`

Update SAML SP Configuration

Updates an existing Service Provider configuration for a SAML client within the Tenant, identified by the application name.

### Example

```go
func callUpdateSAMLSPClientConfigurationByAppName(ctx context.Context, client *Client) {
  samlApp := "<samlApp>" //Required
  samlSpConfigModel := NewSamlSpConfigModel("<provider>", "<dataMap>") //Required

  resp, _, err := client.SAMLCustomProviders.UpdateSAMLSPClientConfigurationByAppName(ctx, samlApp).SamlSpConfigModel(*samlSpConfigModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `samlApp` | path | string | yes | The SAML App identifier |

### Request body

`SamlSpConfigModel` as `application/json`.

### Returns

`SamlSpConfig`


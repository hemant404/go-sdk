<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Social Providers

7 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `DeleteSocialProviderByName`

**DELETE** `/v2/manage/providers/{provider}`

Delete social provider configuration

Deletes the social provider configuration for a specified provider name for the Tenant.

### Example

```go
func callDeleteSocialProviderByName(ctx context.Context, client *Client) {
  provider := "<provider>" //Required

  resp, _, err := client.SocialProviders.DeleteSocialProviderByName(ctx, provider).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `provider` | path | string | yes | Provider Name |

### Returns

`DeleteResponse`

---

## `GetAllProviderConfigurations`

**GET** `/v2/manage/providers`

List social provider configurations

Retrieves all social provider configurations available for the Tenant.

### Example

```go
func callGetAllProviderConfigurations(ctx context.Context, client *Client) {
  resp, _, err := client.SocialProviders.GetAllProviderConfigurations(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `GetEnabledProviders`

**GET** `/v2/manage/providers/active`

Retrieve enabled social providers

Retrieves a list of all enabled social providers for the Tenant.

### Example

```go
func callGetEnabledProviders(ctx context.Context, client *Client) {
  resp, _, err := client.SocialProviders.GetEnabledProviders(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `GetSocialProviderByName`

**GET** `/v2/manage/providers/{provider}`

Retrieve social provider configuration

Retrieves the social provider configuration for a specified provider name for the Tenant.

### Example

```go
func callGetSocialProviderByName(ctx context.Context, client *Client) {
  provider := "<provider>" //Required

  resp, _, err := client.SocialProviders.GetSocialProviderByName(ctx, provider).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `provider` | path | string | yes | Provider Name |

### Returns

`AppProvider`

---

## `SetProvidersOrder`

**PUT** `/v2/manage/providers/setorder`

Set social provider order

Sets the order of social providers for the Tenant to be listed in the UI.

### Example

```go
func callSetProvidersOrder(ctx context.Context, client *Client) {
  setProvidersOrderRequest := SetProvidersOrderRequest{} //Optional
  setProvidersOrderRequest.SetData("<data>")

  resp, _, err := client.SocialProviders.SetProvidersOrder(ctx).SetProvidersOrderRequest(setProvidersOrderRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`object` as `application/json`.

### Returns

`object`

---

## `SetProvidersStatus`

**PUT** `/v2/manage/providers`

Set social provider status

Sets the status of social providers for the Tenant.

### Example

```go
func callSetProvidersStatus(ctx context.Context, client *Client) {
  providerStatusList := ProviderStatusList{} //Optional
  providerStatusList.SetData("<data>")

  resp, _, err := client.SocialProviders.SetProvidersStatus(ctx).ProviderStatusList(providerStatusList).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`ProviderStatusList` as `application/json`.

### Returns

`object`

---

## `UpdateSocialProviderByName`

**PUT** `/v2/manage/providers/{provider}`

Update social provider configuration

Updates the social provider configuration for a specified provider name for the Tenant.

### Example

```go
func callUpdateSocialProviderByName(ctx context.Context, client *Client) {
  provider := "<provider>" //Required
  appProvider := AppProvider{} //Optional
  appProvider.SetIsActive(true)
  appProvider.SetKey("<key>")
  appProvider.SetSecret("<secret>")

  resp, _, err := client.SocialProviders.UpdateSocialProviderByName(ctx, provider).AppProvider(appProvider).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `provider` | path | string | yes | Provider Name |

### Request body

`AppProvider` as `application/json`.

### Returns

`AppProvider`


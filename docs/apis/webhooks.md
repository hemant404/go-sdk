<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Webhooks

6 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `CreateWebhookConfiguration`

**POST** `/v2/manage/webhooks`

Create webhook configuration

Creates a new webhook configuration for the Tenant, allowing registration of a webhook with details such as the Target URL and subscribed events.

### Example

```go
func callCreateWebhookConfiguration(ctx context.Context, client *Client) {
  webhookSubscriptionCreateModel := NewWebhookSubscriptionCreateModel("<event>", "<targetUrl>") //Optional

  resp, _, err := client.Webhooks.CreateWebhookConfiguration(ctx).WebhookSubscriptionCreateModel(*webhookSubscriptionCreateModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`WebhookSubscriptionCreateModel` as `application/json`.

### Returns

`WebhookSubscription`

---

## `DeleteWebhookConfigurationById`

**DELETE** `/v2/manage/webhooks/{hookId}`

Delete webhook configuration

Deletes a specific webhook configuration for the Tenant using its unique ID, permanently removing the webhook from receiving further event notifications.

### Example

```go
func callDeleteWebhookConfigurationById(ctx context.Context, client *Client) {
  hookId := "<hookId>" //Required

  resp, _, err := client.Webhooks.DeleteWebhookConfigurationById(ctx, hookId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `hookId` | path | string | yes | Webhook ID |

### Returns

`DeleteResponse`

---

## `GetAllEvents`

**GET** `/v2/manage/webhooks/events`

List webhook events

Retrieves a list of all available webhook events that can be subscribed to by the Tenant for configuring webhooks to receive notifications for specific activities.

### Example

```go
func callGetAllEvents(ctx context.Context, client *Client) {
  resp, _, err := client.Webhooks.GetAllEvents(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`WebhookEvents`

---

## `GetAllWebhooksConfigurations`

**GET** `/v2/manage/webhooks`

List webhook configurations

Retrieves a list of all configured webhooks for the Tenant, including detailed information about each webhook and its subscribed events.

### Example

```go
func callGetAllWebhooksConfigurations(ctx context.Context, client *Client) {
  resp, _, err := client.Webhooks.GetAllWebhooksConfigurations(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`WebhookSubscriptionResponse`

---

## `GetWebhookConfigurationById`

**GET** `/v2/manage/webhooks/{hookId}`

Retrieve webhook configuration

Retrieves the details of a specific webhook configuration for the Tenant by its unique ID, including the Target URL, subscribed events, and other settings.

### Example

```go
func callGetWebhookConfigurationById(ctx context.Context, client *Client) {
  hookId := "<hookId>" //Required

  resp, _, err := client.Webhooks.GetWebhookConfigurationById(ctx, hookId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `hookId` | path | string | yes | Webhook ID |

### Returns

`WebhookSubscription`

---

## `UpdateWebhookConfigurationById`

**PUT** `/v2/manage/webhooks/{hookId}`

Update webhook configuration

Updates an existing webhook configuration for the Tenant by its unique ID, modifying details such as the Target URL, subscribed events, or other settings.

### Example

```go
func callUpdateWebhookConfigurationById(ctx context.Context, client *Client) {
  hookId := "<hookId>" //Required
  webhookSubscriptionUpdateModel := NewWebhookSubscriptionUpdateModel("<targetUrl>") //Optional

  resp, _, err := client.Webhooks.UpdateWebhookConfigurationById(ctx, hookId).WebhookSubscriptionUpdateModel(*webhookSubscriptionUpdateModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `hookId` | path | string | yes | Webhook ID |

### Request body

`WebhookSubscriptionUpdateModel` as `application/json`.

### Returns

`WebhookSubscription`


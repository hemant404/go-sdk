<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Password Policy

2 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `GetPasswordPolicy`

**GET** `/v2/manage/password-policies`

Retrieve Password policy

Retrieves the Password policy settings for a specific Tenant.

### Example

```go
func callGetPasswordPolicy(ctx context.Context, client *Client) {
  resp, _, err := client.PasswordPolicy.GetPasswordPolicy(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`PasswordPolicy`

---

## `UpdatePasswordPolicy`

**PUT** `/v2/manage/password-policies`

Update Password policy

Updates the Password policy settings for a specific Tenant.

### Example

```go
func callUpdatePasswordPolicy(ctx context.Context, client *Client) {
  passwordPolicy := PasswordPolicy{} //Required
  passwordPolicy.SetDictionaryPasswordValidation(true)
  passwordPolicy.SetProfileDataPasswordValidation(true)
  passwordPolicy.SetProfileDataPasswordExactMatch(true)

  resp, _, err := client.PasswordPolicy.UpdatePasswordPolicy(ctx).PasswordPolicy(passwordPolicy).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`PasswordPolicy` as `application/json`.

### Returns

`PasswordPolicy`


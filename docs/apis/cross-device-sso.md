<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Cross Device SSO

3 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `GenerateQRCode`

**GET** `/sso/mobile/generate`

Retrieve QR code

Retrieves a QR code for Cross Device SSO.

### Example

```go
func callGenerateQRCode(ctx context.Context, client *Client) {
  expiry := "<expiry>" //Optional

  resp, _, err := client.CrossDeviceSSO.GenerateQRCode(ctx).Expiry(expiry).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `expiry` | query | string | no | Code Expiry time (in second) in second, Min:0, Max:300 |

### Returns

`QRCodeResponse`

---

## `GetAccessTokenByPing`

**GET** `/sso/mobile/token`

Retrieve Access Token by ping

Retrieves an Access Token by ping after a User scans a QR code during mobile login.

### Example

```go
func callGetAccessTokenByPing(ctx context.Context, client *Client) {
  code := "<code>" //Optional

  resp, _, err := client.CrossDeviceSSO.GetAccessTokenByPing(ctx).Code(code).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `code` | query | string | no | QR Code By Generate QR Code API |

### Returns

`AccessTokenByPingQRCodeResponse`

---

## `MapQRCodeToAccessToken`

**POST** `/sso/mobile/token`

Map QR code to Access Token

Maps a scanned QR code to an Access Token during mobile login.

### Example

```go
func callMapQRCodeToAccessToken(ctx context.Context, client *Client) {
  qRCodeMapToToken := QRCodeMapToToken{} //Required
  qRCodeMapToToken.SetAccessToken("<accessToken>")
  qRCodeMapToToken.SetCode("<code>")

  resp, _, err := client.CrossDeviceSSO.MapQRCodeToAccessToken(ctx).QRCodeMapToToken(qRCodeMapToToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`QRCodeMapToToken` as `application/json`.

### Returns

`QRCodeMapToTokenResponse`


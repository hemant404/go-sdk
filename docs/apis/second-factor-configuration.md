<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Second Factor Configuration

6 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `GetDuoAuthenticatorConfiguration`

**GET** `/v2/manage/2fa/duo-authenticator-settings`

Retrieve Duo configuration

Retrieves the Duo Authentication configuration for a specific Tenant.

### Example

```go
func callGetDuoAuthenticatorConfiguration(ctx context.Context, client *Client) {
  resp, _, err := client.SecondFactorConfiguration.GetDuoAuthenticatorConfiguration(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`DuoSecurityAuthenticator`

---

## `GetSecondFactorConfiguration`

**GET** `/v2/manage/2fa/config`

Retrieve second factor configuration

Retrieves the second factor authentication configuration for the Tenant.

### Example

```go
func callGetSecondFactorConfiguration(ctx context.Context, client *Client) {
  resp, _, err := client.SecondFactorConfiguration.GetSecondFactorConfiguration(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`MFASettings`

---

## `GetTOTPConfiguration`

**GET** `/v2/manage/2fa/totp-authenticator-settings`

Retrieve TOTP configuration

Retrieves the Time-based One Time Password (TOTP) configuration for a specific Tenant.

### Example

```go
func callGetTOTPConfiguration(ctx context.Context, client *Client) {
  resp, _, err := client.SecondFactorConfiguration.GetTOTPConfiguration(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`GoogleAuthenticator`

---

## `UpdateDuoAuthenticatorConfiguration`

**PUT** `/v2/manage/2fa/duo-authenticator-settings`

Update Duo configuration

Updates the Duo Authentication configuration for a specific Tenant.

### Example

```go
func callUpdateDuoAuthenticatorConfiguration(ctx context.Context, client *Client) {
  duoSecurityAuthenticator := DuoSecurityAuthenticator{} //Required
  duoSecurityAuthenticator.SetIsEnabled(true)
  duoSecurityAuthenticator.SetClientId("<clientId>")
  duoSecurityAuthenticator.SetClientSecret("<clientSecret>")

  resp, _, err := client.SecondFactorConfiguration.UpdateDuoAuthenticatorConfiguration(ctx).DuoSecurityAuthenticator(duoSecurityAuthenticator).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`DuoSecurityAuthenticator` as `application/json`.

### Returns

`DuoSecurityAuthenticator`

---

## `UpdateSecondFactorConfiguration`

**PUT** `/v2/manage/2fa/config`

Update second factor configuration

Updates the second factor authentication configuration for the Tenant.

### Example

```go
func callUpdateSecondFactorConfiguration(ctx context.Context, client *Client) {
  mFASettings := MFASettings{} //Required
  mFASettings.SetIsSecondFactorAuthenticatorEnabled(true)
  mFASettings.SetIsRequired(true)
  mFASettings.SetIsAuthenticatorEnabled(true)

  resp, _, err := client.SecondFactorConfiguration.UpdateSecondFactorConfiguration(ctx).MFASettings(mFASettings).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`MFASettings` as `application/json`.

### Returns

`MFASettings`

---

## `UpdateTOTPConfiguration`

**PUT** `/v2/manage/2fa/totp-authenticator-settings`

Update TOTP configuration

Updates the Time-based One Time Password (TOTP) configuration for a specific Tenant.

### Example

```go
func callUpdateTOTPConfiguration(ctx context.Context, client *Client) {
  googleAuthenticator := NewGoogleAuthenticator("<issuerId>") //Required

  resp, _, err := client.SecondFactorConfiguration.UpdateTOTPConfiguration(ctx).GoogleAuthenticator(*googleAuthenticator).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`GoogleAuthenticator` as `application/json`.

### Returns

`GoogleAuthenticator`


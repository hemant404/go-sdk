<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# IP Access Restrictions

3 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `GetIPAccessRestrictions`

**GET** `/v2/manage/restrictions/ip-access`

Retrieve IP Access Restrictions

Retrieves the IP access restrictions configured for a specific Tenant.

### Example

```go
func callGetIPAccessRestrictions(ctx context.Context, client *Client) {
  resp, _, err := client.IPAccessRestrictions.GetIPAccessRestrictions(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`IPAccessRestrictions`

---

## `ResetIPAccessRestrictions`

**DELETE** `/v2/manage/restrictions/ip-access`

Reset IP Access Restrictions

Resets the IP access restrictions to their default state.

### Example

```go
func callResetIPAccessRestrictions(ctx context.Context, client *Client) {
  resp, _, err := client.IPAccessRestrictions.ResetIPAccessRestrictions(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`DeleteResponse`

---

## `UpdateIPAccessRestrictions`

**PUT** `/v2/manage/restrictions/ip-access`

Update IP Access Restrictions

Updates the IP access restrictions for a specific Tenant.

### Example

```go
func callUpdateIPAccessRestrictions(ctx context.Context, client *Client) {
  iPAccessRestrictions := IPAccessRestrictions{} //Required
  iPAccessRestrictions.SetAllowedIPs("<allowedIPs>")
  iPAccessRestrictions.SetDeniedIPs("<deniedIPs>")

  resp, _, err := client.IPAccessRestrictions.UpdateIPAccessRestrictions(ctx).IPAccessRestrictions(iPAccessRestrictions).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`IPAccessRestrictions` as `application/json`.

### Returns

`IPAccessRestrictions`


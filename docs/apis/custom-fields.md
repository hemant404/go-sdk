<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Custom Fields

7 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `CreateCustomField`

**POST** `/v2/manage/custom-fields`

Create custom field

Creates a new Custom Field for the Tenant.

### Example

```go
func callCreateCustomField(ctx context.Context, client *Client) {
  raasCustomFieldModel := NewRaasCustomFieldModel("<customField>") //Required

  resp, _, err := client.CustomFields.CreateCustomField(ctx).RaasCustomFieldModel(*raasCustomFieldModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`RaasCustomFieldModel` as `application/json`.

### Returns

`RaasCustomField`

---

## `DeleteCustomField`

**DELETE** `/v2/manage/custom-fields/{cfname}`

Delete custom field

Deletes a Custom Field by name for the Tenant.

### Example

```go
func callDeleteCustomField(ctx context.Context, client *Client) {
  cfname := "<cfname>" //Required

  resp, _, err := client.CustomFields.DeleteCustomField(ctx, cfname).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `cfname` | path | string | yes | Custom Fields Name |

### Returns

`DeleteResponse`

---

## `GetActiveCustomFields`

**GET** `/v2/manage/custom-fields/active`

List active custom fields

Retrieves all custom fields currently active in the registration form for the Tenant.

### Example

```go
func callGetActiveCustomFields(ctx context.Context, client *Client) {
  resp, _, err := client.CustomFields.GetActiveCustomFields(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `GetAllCustomFields`

**GET** `/v2/manage/custom-fields`

List custom fields

Retrieves all Custom Fields created for the Tenant.

### Example

```go
func callGetAllCustomFields(ctx context.Context, client *Client) {
  resp, _, err := client.CustomFields.GetAllCustomFields(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `GetCustomFieldLimit`

**GET** `/v2/manage/custom-fields/limit`

Retrieve custom field limit

Retrieves the Custom Field Limit configured for the Tenant.

### Example

```go
func callGetCustomFieldLimit(ctx context.Context, client *Client) {
  resp, _, err := client.CustomFields.GetCustomFieldLimit(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`CustomFieldLimitResponse`

---

## `ListCustomFields`

**GET** `/v2/manage/custom-fields/list`

List custom fields

Retrieves all custom fields for the Tenant, returned as an array of strings.

### Example

```go
func callListCustomFields(ctx context.Context, client *Client) {
  resp, _, err := client.CustomFields.ListCustomFields(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `SetCustomField`

**PUT** `/v2/manage/custom-fields`

Set custom field

Updates or sets a Custom Field instance in RAAS to be displayed on forms for the Tenant.

### Example

```go
func callSetCustomField(ctx context.Context, client *Client) {
  setCustomFieldRequest := SetCustomFieldRequest{} //Required
  setCustomFieldRequest.SetData("<data>")

  resp, _, err := client.CustomFields.SetCustomField(ctx).SetCustomFieldRequest(setCustomFieldRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`object` as `application/json`.

### Returns

`object`


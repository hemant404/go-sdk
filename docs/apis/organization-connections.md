<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Organization Connections

6 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `CreateOrganizationConnection`

**POST** `/v2/manage/organizations/{orgId}/connections`

Create Organization connection

Creates a new Identity Provider connection for an Organization.

### Example

```go
func callCreateOrganizationConnection(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required
  organizationConnectionCreateRequest := OrganizationConnectionCreateRequest{} //Required

  resp, _, err := client.OrganizationConnections.CreateOrganizationConnection(ctx, orgId).OrganizationConnectionCreateRequest(organizationConnectionCreateRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Request body

`OrganizationConnectionCreateRequest` as `application/json`.

### Returns

`ConnectionResponse`

---

## `DeleteOrganizationConnection`

**DELETE** `/v2/manage/organizations/{orgId}/connections/{connId}`

Delete Organization connection

Deletes an Identity Provider connection from an Organization.

### Example

```go
func callDeleteOrganizationConnection(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required
  connId := "<connId>" //Required

  resp, _, err := client.OrganizationConnections.DeleteOrganizationConnection(ctx, orgId, connId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `connId` | path | string | yes | Organization Connection ID |

### Returns

`DeleteResponse`

---

## `GetAllOrganizationConnections`

**GET** `/v2/manage/organizations/{orgId}/connections`

List Organization connections

Lists all Identity Provider connections for an Organization.

### Example

```go
func callGetAllOrganizationConnections(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required

  resp, _, err := client.OrganizationConnections.GetAllOrganizationConnections(ctx, orgId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`object`

---

## `GetOrganizationConnection`

**GET** `/v2/manage/organizations/{orgId}/connections/{connId}`

Retrieve Organization connection

Retrieves details of a specific Identity Provider connection.

### Example

```go
func callGetOrganizationConnection(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required
  connId := "<connId>" //Required

  resp, _, err := client.OrganizationConnections.GetOrganizationConnection(ctx, orgId, connId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `connId` | path | string | yes | Organization Connection ID |

### Returns

`ConnectionResponse`

---

## `UpdateConnectionStatus`

**PUT** `/v2/manage/organizations/{orgId}/connections/{connId}/status`

Update Organization connection status

Updates the active status of an Identity Provider connection.

### Example

```go
func callUpdateConnectionStatus(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required
  connId := "<connId>" //Required
  connectionStatusRequest := ConnectionStatusRequest{} //Required
  connectionStatusRequest.SetActive(true)

  resp, _, err := client.OrganizationConnections.UpdateConnectionStatus(ctx, orgId, connId).ConnectionStatusRequest(connectionStatusRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `connId` | path | string | yes | Organization Connection ID |

### Request body

`ConnectionStatusRequest` as `application/json`.

### Returns

`ConnectionStatusResponse`

---

## `UpdateOrganizationConnection`

**PUT** `/v2/manage/organizations/{orgId}/connections/{connId}`

Update Organization connection

Updates the configuration of an Identity Provider connection.

### Example

```go
func callUpdateOrganizationConnection(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required
  connId := "<connId>" //Required
  organizationConnectionRequest := OrganizationConnectionRequest{} //Required

  resp, _, err := client.OrganizationConnections.UpdateOrganizationConnection(ctx, orgId, connId).OrganizationConnectionRequest(organizationConnectionRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |
| `connId` | path | string | yes | Organization Connection ID |

### Request body

`OrganizationConnectionRequest` as `application/json`.

### Returns

`ConnectionResponse`


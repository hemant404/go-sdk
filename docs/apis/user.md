<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# User

33 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AccountListPasskey`

**GET** `/identity/v2/auth/account/passkey`

List registered Passkeys

Lists all registered Passkeys for a User with a valid Access Token.

### Example

```go
func callAccountListPasskey(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional

  resp, _, err := client.User.AccountListPasskey(ctx).AccessToken(accessToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |

### Returns

`PasskeyListResponse`

---

## `AccountRemovePasskey`

**DELETE** `/identity/v2/auth/account/passkey/{passkeyId}`

Remove Passkey

Removes a specific Passkey from the User's Account.

### Example

```go
func callAccountRemovePasskey(ctx context.Context, client *Client) {
  passkeyId := "<passkeyId>" //Required
  accessToken := "<accessToken>" //Optional

  resp, _, err := client.User.AccountRemovePasskey(ctx, passkeyId).AccessToken(accessToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `passkeyId` | path | string | yes | Id asscociated with the Passkey |
| `access_token` | query | string | no | Access Token of the User |

### Returns

`IsDeleted`

---

## `AddEmail`

**POST** `/identity/v2/auth/email`

Add Email

Adds an Email to a User's account, either as a primary or additional Email.

### Example

```go
func callAddEmail(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  emailtemplate := "<emailtemplate>" //Optional
  verificationurl := "<verificationurl>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  addEmailModel := NewAddEmailModel("<email>") //Required

  resp, _, err := client.User.AddEmail(ctx).AccessToken(accessToken).Emailtemplate(emailtemplate).Verificationurl(verificationurl).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).AddEmailModel(*addEmailModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `emailtemplate` | query | string | no | Name of the Email template to use for this notification. |
| `verificationurl` | query | string | no | Verification URL for the User which will be included in the Email template.. |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Request body

`AddEmailModel` as `application/json`.

### Returns

`IsPostedResponse`

---

## `ChangePhoneNumber`

**PUT** `/identity/v2/auth/phone`

Change Phone number

Updates the User's Phone number using the Access Token.

### Example

```go
func callChangePhoneNumber(ctx context.Context, client *Client) {
  smstemplate := "<smstemplate>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  accessToken := "<accessToken>" //Optional
  isvoiceotp := true //Optional
  phoneIdModel := NewPhoneIdModel("<phone>") //Optional

  resp, _, err := client.User.ChangePhoneNumber(ctx).Smstemplate(smstemplate).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).AccessToken(accessToken).Isvoiceotp(isvoiceotp).PhoneIdModel(*phoneIdModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `smstemplate` | query | string | no | SMS Template |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |
| `access_token` | query | string | no | Access Token of the User |
| `isvoiceotp` | query | boolean | no | Boolean flag to enforce sending SMS content via Voice. |

### Request body

`PhoneIdModel` as `application/json`.

### Returns

`SMSResponse`

---

## `CheckEmailAvailability`

**GET** `/identity/v2/auth/email`

Check Email availability

Verifies Email availability or checks Email using a Verification Token or OTP.

### Example

```go
func callCheckEmailAvailability(ctx context.Context, client *Client) {
  email := "<email>" //Optional
  username := "<username>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  verificationtoken := "<verificationtoken>" //Optional
  otp := "<otp>" //Optional
  uuid := "<uuid>" //Optional
  url := "<url>" //Optional
  welcomeemailtemplate := "<welcomeemailtemplate>" //Optional
  gRecaptchaResponse := "<gRecaptchaResponse>" //Optional
  gRecaptchaResponse2 := "<gRecaptchaResponse2>" //Optional
  qqCaptchaTicket := "<qqCaptchaTicket>" //Optional
  qqCaptchaRandstr := "<qqCaptchaRandstr>" //Optional
  hCaptchaResponse := "<hCaptchaResponse>" //Optional

  resp, _, err := client.User.CheckEmailAvailability(ctx).Email(email).Username(username).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).Verificationtoken(verificationtoken).Otp(otp).Uuid(uuid).Url(url).Welcomeemailtemplate(welcomeemailtemplate).GRecaptchaResponse(gRecaptchaResponse).GRecaptchaResponse2(gRecaptchaResponse2).QqCaptchaTicket(qqCaptchaTicket).QqCaptchaRandstr(qqCaptchaRandstr).HCaptchaResponse(hCaptchaResponse).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `email` | query | string (email) | no | Email address of the associated Account. |
| `username` | query | string | no | Username of the associated Account. |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |
| `verificationtoken` | query | string | no | Verification token received in the Email. |
| `otp` | query | string | no | One-time passcode sent to the User's Email. |
| `uuid` | query | string | no | Email template for the welcome Email. |
| `url` | query | string | no | URL to log the main domain in the database. |
| `welcomeemailtemplate` | query | string | no | Welcome Email Template |
| `g-recaptcha-response` | query | string | no | Google reCAPTCHA response parameter which will be sent to the server for verification. |
| `g_recaptcha_response` | query | string | no | Google reCAPTCHA Response |
| `qq_captcha_ticket` | query | string | no | QQ reCAPTCHA Response |
| `qq_captcha_randstr` | query | string | no | QQ reCAPTCHA Response |
| `h-captcha-response` | query | string | no | hCaptcha Response |

### Returns

`object`

---

## `DeleteAccByPhoneOTP`

**POST** `/identity/v2/auth/account/delete`

Delete Account by Phone OTP

Deletes an Account using a Phone OTP.

### Example

```go
func callDeleteAccByPhoneOTP(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  gRecaptchaResponse := "<gRecaptchaResponse>" //Optional
  gRecaptchaResponse2 := "<gRecaptchaResponse2>" //Optional
  hCaptchaResponse := "<hCaptchaResponse>" //Optional
  qqCaptchaTicket := "<qqCaptchaTicket>" //Optional
  qqCaptchaRandstr := "<qqCaptchaRandstr>" //Optional
  verifyDeleteAccountOtp := NewVerifyDeleteAccountOtp("<otp>") //Optional

  resp, _, err := client.User.DeleteAccByPhoneOTP(ctx).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).GRecaptchaResponse(gRecaptchaResponse).GRecaptchaResponse2(gRecaptchaResponse2).HCaptchaResponse(hCaptchaResponse).QqCaptchaTicket(qqCaptchaTicket).QqCaptchaRandstr(qqCaptchaRandstr).VerifyDeleteAccountOtp(*verifyDeleteAccountOtp).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |
| `g-recaptcha-response` | query | string | no | Google reCAPTCHA response parameter which will be sent to the server for verification. |
| `g_recaptcha_response` | query | string | no | Google reCAPTCHA Response |
| `h-captcha-response` | query | string | no | hCaptcha Response |
| `qq_captcha_ticket` | query | string | no | QQ Captcha ticket (required if Bot Protection is enabled) |
| `qq_captcha_randstr` | query | string | no | QQ Captcha rand string (required if Bot Protection is enabled) |

### Request body

`VerifyDeleteAccountOtp` as `application/json`.

### Returns

`IsDeleted`

---

## `DeleteAccount`

**GET** `/identity/v2/auth/account/delete`

Delete Account by Email token or OTP

Deletes an Account using a delete token or OTP.

### Example

```go
func callDeleteAccount(ctx context.Context, client *Client) {
  preventWebhook := true //Optional
  deletetoken := "<deletetoken>" //Optional
  xPreventWebhook := true //Optional
  email := "<email>" //Optional
  otp := "<otp>" //Optional

  resp, _, err := client.User.DeleteAccount(ctx).PreventWebhook(preventWebhook).Deletetoken(deletetoken).XPreventWebhook(xPreventWebhook).Email(email).Otp(otp).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `deletetoken` | query | string | no | This is required if the OTP is not passed in the query parameter. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |
| `email` | query | string (email) | no | Email address of the associated Account. |
| `otp` | query | string | no | One-time passcode sent to the User's Email. |

### Returns

`IsPostedResponse`

---

## `DeleteAccountByAccessToken`

**DELETE** `/identity/v2/auth/account`

Send User deletion Email

Sends a confirmation Email for User deletion to the User's Email using their Access Token.

### Example

```go
func callDeleteAccountByAccessToken(ctx context.Context, client *Client) {
  emailtemplate := "<emailtemplate>" //Optional
  deleteurl := "<deleteurl>" //Optional
  accessToken := "<accessToken>" //Optional

  resp, _, err := client.User.DeleteAccountByAccessToken(ctx).Emailtemplate(emailtemplate).Deleteurl(deleteurl).AccessToken(accessToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `emailtemplate` | query | string | no | Name of the Email template to use for this notification. |
| `deleteurl` | query | string | no | DeleteUrl URL which is being sent in the Email |
| `access_token` | query | string | no | Access Token of the User |

### Returns

`IsDeleteRequestAccepted`

---

## `Deleteemailbyaccesstoken`

**DELETE** `/identity/v2/auth/email`

Remove Email

Removes additional Emails from a User's account.

### Example

```go
func callDeleteemailbyaccesstoken(ctx context.Context, client *Client) {
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  deleteemailbyaccesstokenRequest := NewDeleteemailbyaccesstokenRequest("<email>") //Required

  resp, _, err := client.User.Deleteemailbyaccesstoken(ctx).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).DeleteemailbyaccesstokenRequest(*deleteemailbyaccesstokenRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Request body

`object` as `application/json`.

### Returns

`IsDeleted`

---

## `GetAccountDetails`

**GET** `/identity/v2/auth/account`

Retrieve User

Retrieves User details based on the Access Token.

### Example

```go
func callGetAccountDetails(ctx context.Context, client *Client) {
  welcomeemailtemplate := "<welcomeemailtemplate>" //Optional
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional

  resp, _, err := client.User.GetAccountDetails(ctx).Welcomeemailtemplate(welcomeemailtemplate).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `welcomeemailtemplate` | query | string | no | Welcome Email Template |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Returns

`IdentityResponseWithSocialWithoutLogins`

---

## `GetConsentLogs`

**GET** `/identity/v2/auth/consent/logs`

Retrieve Consent Logs

Retrieves consent logs for a User based on the provided Access Token.

### Example

```go
func callGetConsentLogs(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional

  resp, _, err := client.User.GetConsentLogs(ctx).AccessToken(accessToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |

### Returns

`ConsentLogsResponse`

---

## `GetInvitation`

**GET** `/identity/v2/auth/invitations/{invitation_token}`

Retrieve invitation details

Retrieves details about a specific invitation using the invitation token.

### Example

```go
func callGetInvitation(ctx context.Context, client *Client) {
  invitationToken := "<invitationToken>" //Required

  resp, _, err := client.User.GetInvitation(ctx, invitationToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `invitation_token` | path | string | yes | The token of the invitation to retrieve. |

### Returns

`InvitationToken`

---

## `GetInvitationByInvitationId`

**GET** `/v2/manage/invitations/{invitationid}`

Retrieve invitation by ID

Retrieves invitation details by invitation ID.

### Example

```go
func callGetInvitationByInvitationId(ctx context.Context, client *Client) {
  invitationid := "<invitationid>" //Required

  resp, _, err := client.User.GetInvitationByInvitationId(ctx, invitationid).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `invitationid` | path | string | yes | The ID of the invitation. The ID is typically in the format *inv_<unique_id>*, where *<unique_id>* is a string of alphanumeric characters. |

### Returns

`Invitation`

---

## `GetPrivacyPolicyAcceptance`

**GET** `/identity/v2/auth/privacypolicy/accept`

Accept Privacy Policy

Updates the Privacy Policy stored in a User's profile using their Access Token.

### Example

```go
func callGetPrivacyPolicyAcceptance(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional

  resp, _, err := client.User.GetPrivacyPolicyAcceptance(ctx).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Returns

`IdentityResponseWithSocialWithoutLogins`

---

## `GetPrivacyPolicyHistory`

**GET** `/identity/v2/auth/privacypolicy/history`

Retrieve Privacy Policy History

Returns all accepted Privacy Policies for a User using their Access Token.

### Example

```go
func callGetPrivacyPolicyHistory(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional

  resp, _, err := client.User.GetPrivacyPolicyHistory(ctx).AccessToken(accessToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |

### Returns

`PrivacyPolicyHistoryResponse`

---

## `GetVerifiedConsentWithAccessToken`

**GET** `/identity/v2/auth/consent/verify`

Retrieve Consent Status

Retrieves the consent verification status for a User based on the provided Access Token and event.

### Example

```go
func callGetVerifiedConsentWithAccessToken(ctx context.Context, client *Client) {
  event := "<event>" //Required
  iscustom := true //Required
  accessToken := "<accessToken>" //Optional

  resp, _, err := client.User.GetVerifiedConsentWithAccessToken(ctx).Event(event).Iscustom(iscustom).AccessToken(accessToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `event` | query | string | yes | Event type to filter consent verification (e.g., `login`). |
| `iscustom` | query | boolean | yes | This field value is used to filter the consent verification by custom events. The iscustom value should be a boolean. If true, it filters for custom events; if false, it filters for standard events. |

### Returns

`VerifyConsent`

---

## `LinkSocialIdentitiesByAccessToken`

**POST** `/identity/v2/auth/socialidentity`

Link social identities

Links a social provider account to an existing Account using Access Tokens.

### Example

```go
func callLinkSocialIdentitiesByAccessToken(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  candidateTokenModel := NewCandidateTokenModel("<candidatetoken>") //Optional

  resp, _, err := client.User.LinkSocialIdentitiesByAccessToken(ctx).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).CandidateTokenModel(*candidateTokenModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Request body

`CandidateTokenModel` as `application/json`.

### Returns

`IsPostedResponse`

---

## `LinkSocialIdentitiesByPing`

**POST** `/identity/v2/auth/socialidentity/ping`

Link social identities via PING

Links a social provider account with an existing Account using the Access Token and the social provider's User Access Token.

### Example

```go
func callLinkSocialIdentitiesByPing(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  clientGuidBodyModel := ClientGuidBodyModel{} //Optional
  clientGuidBodyModel.SetClientgUID("<clientgUID>")
  clientGuidBodyModel.SetAccessToken("<accessToken>")

  resp, _, err := client.User.LinkSocialIdentitiesByPing(ctx).AccessToken(accessToken).ClientGuidBodyModel(clientGuidBodyModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |

### Request body

`ClientGuidBodyModel` as `application/json`.

### Returns

`IsPostedResponse`

---

## `RemovePhoneIdByToken`

**DELETE** `/identity/v2/auth/phone`

Remove Phone number

Removes the User's Phone number using the Access Token.

### Example

```go
func callRemovePhoneIdByToken(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional

  resp, _, err := client.User.RemovePhoneIdByToken(ctx).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Returns

`IsDeleted`

---

## `ResendEmailVerification`

**PUT** `/identity/v2/auth/register`

Resend verification Email

Resends the verification Email to the User to confirm their Email address.

### Example

```go
func callResendEmailVerification(ctx context.Context, client *Client) {
  verificationurl := "<verificationurl>" //Optional
  emailtemplate := "<emailtemplate>" //Optional
  emailModel := NewEmailModel("<email>") //Required

  resp, _, err := client.User.ResendEmailVerification(ctx).Verificationurl(verificationurl).Emailtemplate(emailtemplate).EmailModel(*emailModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `verificationurl` | query | string | no | Verification URL for the User which will be included in the Email template.. |
| `emailtemplate` | query | string | no | Name of the Email template to use for this notification. |

### Request body

`EmailModel` as `application/json`.

### Returns

`IsPostedResponse`

---

## `ResendPhoneOtp`

**POST** `/identity/v2/auth/phone/otp`

Resend Phone OTP

Resends the Phone OTP using either the Access Token or Phone number.

### Example

```go
func callResendPhoneOtp(ctx context.Context, client *Client) {
  smstemplate := "<smstemplate>" //Optional
  accessToken := "<accessToken>" //Optional
  isvoiceotp := true //Optional
  phoneIdModelOptional := PhoneIdModelOptional{} //Optional
  phoneIdModelOptional.SetPhone("<phone>")

  resp, _, err := client.User.ResendPhoneOtp(ctx).Smstemplate(smstemplate).AccessToken(accessToken).Isvoiceotp(isvoiceotp).PhoneIdModelOptional(phoneIdModelOptional).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `smstemplate` | query | string | no | SMS Template |
| `access_token` | query | string | no | Access Token of the User |
| `isvoiceotp` | query | boolean | no | Boolean flag to enforce sending SMS content via Voice. |

### Request body

`PhoneIdModelOptional` as `application/json`.

### Returns

`SMSResponse`

---

## `SendDeleteOtp`

**GET** `/identity/v2/auth/account/otp`

Retrieve delete Account OTP

Retrieves the OTP for the specified Account to facilitate account deletion.

### Example

```go
func callSendDeleteOtp(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  smstemplate := "<smstemplate>" //Optional
  isvoiceotp := true //Optional

  resp, _, err := client.User.SendDeleteOtp(ctx).AccessToken(accessToken).Smstemplate(smstemplate).Isvoiceotp(isvoiceotp).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `smstemplate` | query | string | no | SMS Template |
| `isvoiceotp` | query | boolean | no | Boolean flag to enforce sending SMS content via Voice. |

### Returns

`SMSResponse`

---

## `SendEmailVerification`

**GET** `/identity/v2/auth/email/sendverificationemail`

Send verification Email for social profile linking

Sends a verification Email to the unverified Email of the social profile. This is applicable only in optional verification workflows.

### Example

```go
func callSendEmailVerification(ctx context.Context, client *Client) {
  emailtemplate := "<emailtemplate>" //Optional
  verificationurl := "<verificationurl>" //Optional
  clientguid := "<clientguid>" //Optional
  accessToken := "<accessToken>" //Optional

  resp, _, err := client.User.SendEmailVerification(ctx).Emailtemplate(emailtemplate).Verificationurl(verificationurl).Clientguid(clientguid).AccessToken(accessToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `emailtemplate` | query | string | no | Name of the Email template to use for this notification. |
| `verificationurl` | query | string | no | Verification URL for the User which will be included in the Email template.. |
| `clientguid` | query | string | no | Client GUID for the request. |
| `access_token` | query | string | no | Access Token of the User |

### Returns

`SendEmailVerificationResponse`

---

## `SendWelcomeEmail`

**GET** `/identity/v2/auth/account/sendwelcomeemail`

Send Welcome Email

Sends a welcome Email to the User.

### Example

```go
func callSendWelcomeEmail(ctx context.Context, client *Client) {
  welcomeemailtemplate := "<welcomeemailtemplate>" //Optional
  accessToken := "<accessToken>" //Optional

  resp, _, err := client.User.SendWelcomeEmail(ctx).Welcomeemailtemplate(welcomeemailtemplate).AccessToken(accessToken).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `welcomeemailtemplate` | query | string | no | Welcome Email Template |
| `access_token` | query | string | no | Access Token of the User |

### Returns

`IsPostedResponse`

---

## `Setorchangeusernamebyaccesstoken`

**PUT** `/identity/v2/auth/username`

Update Username

Sets or changes the User's Username using the Access Token.

### Example

```go
func callSetorchangeusernamebyaccesstoken(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  setUserNameRequest := NewSetUserNameRequest("<username>") //Optional

  resp, _, err := client.User.Setorchangeusernamebyaccesstoken(ctx).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).SetUserNameRequest(*setUserNameRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Request body

`SetUserNameRequest` as `application/json`.

### Returns

`IsPostedResponse`

---

## `SubmitConsentByAccessToken`

**POST** `/identity/v2/auth/consent/profile`

Submit Consent

Submits User consent information using an Access Token.

### Example

```go
func callSubmitConsentByAccessToken(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  consentSubmit := NewConsentSubmit("<events>", "<data>") //Required

  resp, _, err := client.User.SubmitConsentByAccessToken(ctx).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).ConsentSubmit(*consentSubmit).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Request body

`ConsentSubmit` as `application/json`.

### Returns

`Profile`

---

## `SubmitConsentByConsentToken`

**POST** `/identity/v2/auth/consent`

Submit Consent with Token

Submits User consent information using a consent token.

### Example

```go
func callSubmitConsentByConsentToken(ctx context.Context, client *Client) {
  consenttoken := "<consenttoken>" //Required
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  consentSubmit := NewConsentSubmit("<events>", "<data>") //Required

  resp, _, err := client.User.SubmitConsentByConsentToken(ctx).Consenttoken(consenttoken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).ConsentSubmit(*consentSubmit).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `consenttoken` | query | string | yes | The consent token for the User. |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Request body

`ConsentSubmit` as `application/json`.

### Returns

`ConsentResponse`

---

## `UnlinkSocialIdentitiesByAccessToken`

**DELETE** `/identity/v2/auth/socialidentity`

Unlink social identities

Unlinks a social provider account from the specified Account using Access Tokens, removing it from the database.

### Example

```go
func callUnlinkSocialIdentitiesByAccessToken(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  unlinkSocialIdentityRequest := NewUnlinkSocialIdentityRequest("<provider>", "<providerid>") //Optional

  resp, _, err := client.User.UnlinkSocialIdentitiesByAccessToken(ctx).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).UnlinkSocialIdentityRequest(*unlinkSocialIdentityRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Request body

`UnlinkSocialIdentityRequest` as `application/json`.

### Returns

`IsDeleted`

---

## `Unlockaccountbyaccesstoken`

**PUT** `/identity/v2/auth/account/unlock`

Unlock User

Unlocks a User's Account with a valid Access Token after successfully passing Bot Protection challenges.

### Example

```go
func callUnlockaccountbyaccesstoken(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  gRecaptchaResponse := "<gRecaptchaResponse>" //Optional
  qqCaptchaTicket := "<qqCaptchaTicket>" //Optional
  qqCaptchaRandstr := "<qqCaptchaRandstr>" //Optional
  hCaptchaResponse := "<hCaptchaResponse>" //Optional
  unlockaccountbyaccesstokenRequest := NewUnlockaccountbyaccesstokenRequest("<securityAnswer>") //Required

  resp, _, err := client.User.Unlockaccountbyaccesstoken(ctx).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).GRecaptchaResponse(gRecaptchaResponse).QqCaptchaTicket(qqCaptchaTicket).QqCaptchaRandstr(qqCaptchaRandstr).HCaptchaResponse(hCaptchaResponse).UnlockaccountbyaccesstokenRequest(*unlockaccountbyaccesstokenRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |
| `g-recaptcha-response` | query | string | no | Google reCAPTCHA response parameter which will be sent to the server for verification. |
| `qq_captcha_ticket` | query | string | no | QQ reCAPTCHA Response |
| `qq_captcha_randstr` | query | string | no | QQ reCAPTCHA Response |
| `h-captcha-response` | query | string | no | hCaptcha Response |

### Request body

`object` as `application/json`.

### Returns

`IsPostedResponse`

---

## `UpdateAccountByAccessToken`

**PUT** `/identity/v2/auth/account`

Update User

Updates the User's account information using a valid Access Token.

### Example

```go
func callUpdateAccountByAccessToken(ctx context.Context, client *Client) {
  verificationurl := "<verificationurl>" //Optional
  emailtemplate := "<emailtemplate>" //Optional
  smstemplate := "<smstemplate>" //Optional
  nullsupport := true //Optional
  isvoiceotp := true //Optional
  fields := "<fields>" //Optional
  gRecaptchaResponse := "<gRecaptchaResponse>" //Optional
  qqCaptchaTicket := "<qqCaptchaTicket>" //Optional
  qqCaptchaRandstr := "<qqCaptchaRandstr>" //Optional
  hCaptchaResponse := "<hCaptchaResponse>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  accessToken := "<accessToken>" //Optional
  updateAccountByAccessTokenRequest := UpdateAccountByAccessTokenRequest{} //Required
  updateAccountByAccessTokenRequest.SetAccessToken("<accessToken>")
  updateAccountByAccessTokenRequest.SetUserName("<userName>")
  updateAccountByAccessTokenRequest.SetPhoneId("<phoneId>")

  resp, _, err := client.User.UpdateAccountByAccessToken(ctx).Verificationurl(verificationurl).Emailtemplate(emailtemplate).Smstemplate(smstemplate).Nullsupport(nullsupport).Isvoiceotp(isvoiceotp).Fields(fields).GRecaptchaResponse(gRecaptchaResponse).QqCaptchaTicket(qqCaptchaTicket).QqCaptchaRandstr(qqCaptchaRandstr).HCaptchaResponse(hCaptchaResponse).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).AccessToken(accessToken).UpdateAccountByAccessTokenRequest(updateAccountByAccessTokenRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `verificationurl` | query | string | no | Verification URL for the User which will be included in the Email template.. |
| `emailtemplate` | query | string | no | Name of the Email template to use for this notification. |
| `smstemplate` | query | string | no | SMS Template |
| `nullsupport` | query | boolean | no | Bool flag, if this flag is sent as true then the fields which are send in payload as null then in the profile as well that will be saved as null only |
| `isvoiceotp` | query | boolean | no | Boolean flag to enforce sending SMS content via Voice. |
| `fields` | query | string | no | Comma-separated list of profile fields to include in the response. |
| `g-recaptcha-response` | query | string | no | Google reCAPTCHA response parameter which will be sent to the server for verification. |
| `qq_captcha_ticket` | query | string | no | QQ reCAPTCHA Response |
| `qq_captcha_randstr` | query | string | no | QQ reCAPTCHA Response |
| `h-captcha-response` | query | string | no | hCaptcha Response |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |
| `access_token` | query | string | no | Access Token of the User |

### Request body

`object` as `application/json`.

### Returns

`UpdateByTokenResponse`

---

## `UpdateConsentByAccessToken`

**PUT** `/identity/v2/auth/consent`

Update Consent Profile

Updates the consent profile using an Access Token.

### Example

```go
func callUpdateConsentByAccessToken(ctx context.Context, client *Client) {
  accessToken := "<accessToken>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  consentUpdate := NewConsentUpdate("<consents>") //Required

  resp, _, err := client.User.UpdateConsentByAccessToken(ctx).AccessToken(accessToken).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).ConsentUpdate(*consentUpdate).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `access_token` | query | string | no | Access Token of the User |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |

### Request body

`ConsentUpdate` as `application/json`.

### Returns

`ConsentProfile`

---

## `UpdateEmail`

**PUT** `/identity/v2/auth/email`

Verify Email

Verifies the User's Email when OTP Email Verification is enabled, requiring LoginRadius activation.

### Example

```go
func callUpdateEmail(ctx context.Context, client *Client) {
  url := "<url>" //Optional
  welcomeemailtemplate := "<welcomeemailtemplate>" //Optional
  preventWebhook := true //Optional
  xPreventWebhook := true //Optional
  gRecaptchaResponse := "<gRecaptchaResponse>" //Optional
  gRecaptchaResponse2 := "<gRecaptchaResponse2>" //Optional
  qqCaptchaTicket := "<qqCaptchaTicket>" //Optional
  qqCaptchaRandstr := "<qqCaptchaRandstr>" //Optional
  hCaptchaResponse := "<hCaptchaResponse>" //Optional
  updateEmailRequest := NewUpdateEmailRequest("<otp>") //Optional

  resp, _, err := client.User.UpdateEmail(ctx).Url(url).Welcomeemailtemplate(welcomeemailtemplate).PreventWebhook(preventWebhook).XPreventWebhook(xPreventWebhook).GRecaptchaResponse(gRecaptchaResponse).GRecaptchaResponse2(gRecaptchaResponse2).QqCaptchaTicket(qqCaptchaTicket).QqCaptchaRandstr(qqCaptchaRandstr).HCaptchaResponse(hCaptchaResponse).UpdateEmailRequest(*updateEmailRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `url` | query | string | no | URL to log the main domain in the database. |
| `welcomeemailtemplate` | query | string | no | Welcome Email Template |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |
| `g-recaptcha-response` | query | string | no | Google reCAPTCHA response parameter which will be sent to the server for verification. |
| `g_recaptcha_response` | query | string | no | Google reCAPTCHA Response |
| `qq_captcha_ticket` | query | string | no | QQ reCAPTCHA Response |
| `qq_captcha_randstr` | query | string | no | QQ reCAPTCHA Response |
| `h-captcha-response` | query | string | no | hCaptcha Response |

### Request body

`object` as `application/json`.

### Returns

`object`

---

## `VerifyPhoneOtp`

**PUT** `/identity/v2/auth/phone/otp`

Verify Phone

Validates the verification code sent to confirm a User's Phone number when the User is logged in and provides an Access Token.

### Example

```go
func callVerifyPhoneOtp(ctx context.Context, client *Client) {
  otp := "<otp>" //Optional
  smstemplate := "<smstemplate>" //Optional
  gRecaptchaResponse := "<gRecaptchaResponse>" //Optional
  gRecaptchaResponse2 := "<gRecaptchaResponse2>" //Optional
  qqCaptchaTicket := "<qqCaptchaTicket>" //Optional
  qqCaptchaRandstr := "<qqCaptchaRandstr>" //Optional
  hCaptchaResponse := "<hCaptchaResponse>" //Optional
  accessToken := "<accessToken>" //Optional
  xPreventWebhook := true //Optional
  preventWebhook := true //Optional
  verifyOtpPhoneModel := NewVerifyOtpPhoneModel("<phone>") //Required

  resp, _, err := client.User.VerifyPhoneOtp(ctx).Otp(otp).Smstemplate(smstemplate).GRecaptchaResponse(gRecaptchaResponse).GRecaptchaResponse2(gRecaptchaResponse2).QqCaptchaTicket(qqCaptchaTicket).QqCaptchaRandstr(qqCaptchaRandstr).HCaptchaResponse(hCaptchaResponse).AccessToken(accessToken).XPreventWebhook(xPreventWebhook).PreventWebhook(preventWebhook).VerifyOtpPhoneModel(*verifyOtpPhoneModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `otp` | query | string | no | One-time passcode sent to the User's Email. |
| `smstemplate` | query | string | no | SMS Template |
| `g-recaptcha-response` | query | string | no | Google reCAPTCHA response parameter which will be sent to the server for verification. |
| `g_recaptcha_response` | query | string | no | Google reCAPTCHA Response |
| `qq_captcha_ticket` | query | string | no | QQ reCAPTCHA Response |
| `qq_captcha_randstr` | query | string | no | QQ reCAPTCHA Response |
| `h-captcha-response` | query | string | no | hCaptcha Response |
| `access_token` | query | string | no | Access Token of the User |
| `X-PreventWebhook` | header | boolean | no | When true, suppresses webhook events for this operation. |
| `prevent_webhook` | query | boolean | no | When true, suppresses webhook events for this operation. |

### Request body

`VerifyOtpPhoneModel` as `application/json`.

### Returns

`object`


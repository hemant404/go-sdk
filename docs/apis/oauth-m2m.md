<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# OAuth M2M

4 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `GenerateM2MToken`

**POST** `/service/oauth/token`

Generate M2M token

Generates a Machine-to-Machine (M2M) token for application authentication.

### Example

```go
func callGenerateM2MToken(ctx context.Context, client *Client) {
  oAuthM2MTokenGenerate := NewOAuthM2MTokenGenerate("<audience>", "<clientId>", "<clientSecret>", "<grantType>") //Required

  resp, _, err := client.OAuthM2M.GenerateM2MToken(ctx).OAuthM2MTokenGenerate(*oAuthM2MTokenGenerate).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`OAuthM2MTokenGenerate` as `application/json`.

### Returns

`OAuthM2MTokenResponse`

---

## `GetM2MJWKSConfig`

**GET** `/service/oauth/jwks`

Retrieve JSON Web Key Set

Retrieves the JSON Web Key Set (JWKS) for verifying token signatures.

### Example

```go
func callGetM2MJWKSConfig(ctx context.Context, client *Client) {
  resp, _, err := client.OAuthM2M.GetM2MJWKSConfig(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`JWKSResponse`

---

## `GetM2MTokenInfo`

**POST** `/service/oauth/introspect`

Retrieve M2M token info

Retrieves information about a Machine-to-Machine (M2M) token.

### Example

```go
func callGetM2MTokenInfo(ctx context.Context, client *Client) {
  oAuthM2MTokenIntrospect := NewOAuthM2MTokenIntrospect("<clientId>", "<clientSecret>", "<token>", "<tokenTypeHint>") //Required

  resp, _, err := client.OAuthM2M.GetM2MTokenInfo(ctx).OAuthM2MTokenIntrospect(*oAuthM2MTokenIntrospect).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`OAuthM2MTokenIntrospect` as `application/json`.

### Returns

`OAuthM2MIntrospectResponse`

---

## `RevokeM2MToken`

**POST** `/service/oauth/revoke`

Revoke M2M token

Revokes a Machine-to-Machine (M2M) token to invalidate it.

### Example

```go
func callRevokeM2MToken(ctx context.Context, client *Client) {
  oAuthM2MTokenRevoke := NewOAuthM2MTokenRevoke("<clientId>", "<clientSecret>", "<token>", "<tokenTypeHint>") //Required

  resp, _, err := client.OAuthM2M.RevokeM2MToken(ctx).OAuthM2MTokenRevoke(*oAuthM2MTokenRevoke).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`OAuthM2MTokenRevoke` as `application/json`.


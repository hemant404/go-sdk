<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Organization

8 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `CreateOrganization`

**POST** `/v2/manage/organizations`

Create Organization

Creates a new Organization in the Tenant.

### Example

```go
func callCreateOrganization(ctx context.Context, client *Client) {
  createOrganizationRequest := NewCreateOrganizationRequest("<name>") //Required

  resp, _, err := client.Organization.CreateOrganization(ctx).CreateOrganizationRequest(*createOrganizationRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`object` as `application/json`.

### Returns

`OrganizationsResponse`

---

## `CreateOrgTenantRole`

**POST** `/v2/manage/organizations/{orgId}/roles`

Create Role in Organization

Creates a Role within an Organization.

### Example

```go
func callCreateOrgTenantRole(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required
  rolePostRequest := NewRolePostRequest("<name>") //Required

  resp, _, err := client.Organization.CreateOrgTenantRole(ctx, orgId).RolePostRequest(*rolePostRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Request body

`RolePostRequest` as `application/json`.

### Returns

`Role`

---

## `DeleteOrganization`

**DELETE** `/v2/manage/organizations/{orgId}`

Delete Organization

Deletes an Organization by its ID.

### Example

```go
func callDeleteOrganization(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required

  resp, _, err := client.Organization.DeleteOrganization(ctx, orgId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`DeleteResponse`

---

## `GetAllOrganizations`

**GET** `/v2/manage/organizations`

List Organizations

Retrieves a list of all Organizations in the Tenant.

### Example

```go
func callGetAllOrganizations(ctx context.Context, client *Client) {
  resp, _, err := client.Organization.GetAllOrganizations(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `GetOrganization`

**GET** `/v2/manage/organizations/{orgId}`

Retrieve Organization details

Retrieves details of a specific Organization by its ID.

### Example

```go
func callGetOrganization(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required

  resp, _, err := client.Organization.GetOrganization(ctx, orgId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`OrganizationsResponse`

---

## `GetOrgContextByOrgId`

**GET** `/v2/manage/organizations/{orgId}/orgcontext`

Retrieve Organization context

Retrieves User Roles for all Organizations by OrgID.

### Example

```go
func callGetOrgContextByOrgId(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required

  resp, _, err := client.Organization.GetOrgContextByOrgId(ctx, orgId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Unique identifier of the Organization. |

### Returns

`object`

---

## `GetOrgRolesByOrgId`

**GET** `/v2/manage/organizations/{orgId}/roles`

List Organization Roles

Lists all Roles defined within an Organization.

### Example

```go
func callGetOrgRolesByOrgId(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required

  resp, _, err := client.Organization.GetOrgRolesByOrgId(ctx, orgId).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`object`

---

## `UpdateOrganization`

**PUT** `/v2/manage/organizations/{orgId}`

Update Organization

Updates an Organization by its ID. Supports updating org fields, policies, and status in a single request.

### Example

```go
func callUpdateOrganization(ctx context.Context, client *Client) {
  orgId := "<orgId>" //Required
  organizationUpdateRequest := OrganizationUpdateRequest{} //Required
  organizationUpdateRequest.SetName("<name>")
  organizationUpdateRequest.SetDisplay("<display>")
  organizationUpdateRequest.SetMetadata("<metadata>")

  resp, _, err := client.Organization.UpdateOrganization(ctx, orgId).OrganizationUpdateRequest(organizationUpdateRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Request body

`OrganizationUpdateRequest` as `application/json`.

### Returns

`OrganizationsResponse`


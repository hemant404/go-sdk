<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Domain Access Restrictions

2 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `GetDomainAccessRestrictionsByAppID`

**GET** `/v2/manage/restrictions/domain-access`

Retrieve Domain Access Restrictions

Retrieves the domain access restrictions configured for the Tenant.

### Example

```go
func callGetDomainAccessRestrictionsByAppID(ctx context.Context, client *Client) {
  resp, _, err := client.DomainAccessRestrictions.GetDomainAccessRestrictionsByAppID(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`DomainAccessRestrictions`

---

## `UpdateDomainAccessRestrictionsByAppID`

**PUT** `/v2/manage/restrictions/domain-access`

Update Domain Access Restrictions

Updates the domain access restrictions for the Tenant.

### Example

```go
func callUpdateDomainAccessRestrictionsByAppID(ctx context.Context, client *Client) {
  domainAccessRestrictions := DomainAccessRestrictions{} //Optional
  domainAccessRestrictions.SetAllowlist("<allowlist>")
  domainAccessRestrictions.SetBlocklist("<blocklist>")

  resp, _, err := client.DomainAccessRestrictions.UpdateDomainAccessRestrictionsByAppID(ctx).DomainAccessRestrictions(domainAccessRestrictions).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`DomainAccessRestrictions` as `application/json`.

### Returns

`DomainAccessRestrictions`


<!-- Generated by sdk-factory (scripts/generate.sh); DO NOT EDIT. -->

# Permissions

5 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AddPermission`

**POST** `/v2/manage/permissions`

Create Permission

Adds a new Permission.

### Example

```go
func callAddPermission(ctx context.Context, client *Client) {
  permissionsPostRequest := NewPermissionsPostRequest("<name>") //Optional

  resp, _, err := client.Permissions.AddPermission(ctx).PermissionsPostRequest(*permissionsPostRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`PermissionsPostRequest` as `application/json`.

### Returns

`Permissions`

---

## `DeleteTenantPermission`

**DELETE** `/v2/manage/permissions/{id}`

Delete Permission

Deletes a specific Permission.

### Example

```go
func callDeleteTenantPermission(ctx context.Context, client *Client) {
  id := "<id>" //Required

  resp, _, err := client.Permissions.DeleteTenantPermission(ctx, id).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `id` | path | string | yes | The unique identifier for the Permission |

### Returns

`DeleteResponse`

---

## `GetPermissionById`

**GET** `/v2/manage/permissions/{id}`

Retrieve Permission by ID

Retrieves a Permission by its ID.

### Example

```go
func callGetPermissionById(ctx context.Context, client *Client) {
  id := "<id>" //Required

  resp, _, err := client.Permissions.GetPermissionById(ctx, id).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `id` | path | string | yes | The unique identifier for the Permission |

### Returns

`Permissions`

---

## `Permissions`

**GET** `/v2/manage/permissions`

List Permissions

Retrieves a list of all Permissions.

### Example

```go
func callPermissions(ctx context.Context, client *Client) {
  resp, _, err := client.Permissions.Permissions(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `UpdateTenantPermission`

**PUT** `/v2/manage/permissions/{id}`

Update Permission

Updates a specific Permission. Note: The Name field cannot be modified for non-B2B apps. If a different Name value is provided, the API will return an error.

### Example

```go
func callUpdateTenantPermission(ctx context.Context, client *Client) {
  id := "<id>" //Required
  permissionPutRequest := NewPermissionPutRequest("<name>", "<description>") //Optional

  resp, _, err := client.Permissions.UpdateTenantPermission(ctx, id).PermissionPutRequest(*permissionPutRequest).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `id` | path | string | yes | The unique identifier for the Permission |

### Request body

`PermissionPutRequest` as `application/json`.

### Returns

`Permissions`


<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Email Templates

4 operation(s). Part of the [API index](../API.md).

Call these as `client.<Service>.<Method>(ctx).Execute()`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `AddEmailTemplate`

**POST** `/v2/manage/email-templates`

Create Email template

Adds a new Email template to the Tenant's configuration.

### Example

```go
func callAddEmailTemplate(ctx context.Context, client *Client) {
  emailTemplateModel := NewEmailTemplateModel("<templateType>", "<template>", "<subject>") //Required

  resp, _, err := client.EmailTemplates.AddEmailTemplate(ctx).EmailTemplateModel(*emailTemplateModel).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Request body

`EmailTemplateModel` as `application/json`.

### Returns

`EmailTemplateResponse`

---

## `DeleteEmailTemplate`

**DELETE** `/v2/manage/email-templates/{templateType}`

Delete Email Template

Deletes the Email template for a specified Email template type within a specific Tenant.

### Example

```go
func callDeleteEmailTemplate(ctx context.Context, client *Client) {
  templateType := "<templateType>" //Required
  deleteEmailTemplate := NewDeleteEmailTemplate("<templateName>") //Optional

  resp, _, err := client.EmailTemplates.DeleteEmailTemplate(ctx, templateType).DeleteEmailTemplate(*deleteEmailTemplate).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `templateType` | path | string | yes | The type of Email template to delete. |

### Request body

`DeleteEmailTemplate` as `application/json`.

### Returns

`DeleteResponse`

---

## `GetEmailTemplates`

**GET** `/v2/manage/email-templates`

List Email templates

Retrieves all Email templates configured for the Tenant.

### Example

```go
func callGetEmailTemplates(ctx context.Context, client *Client) {
  resp, _, err := client.EmailTemplates.GetEmailTemplates(ctx).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Returns

`object`

---

## `UpdateEmailTemplate`

**PUT** `/v2/manage/email-templates/{templateType}`

Update Email Template

Updates the Email template for a specified Email template type within a specific Tenant.

### Example

```go
func callUpdateEmailTemplate(ctx context.Context, client *Client) {
  templateType := "<templateType>" //Required
  updateEmailTemplate := NewUpdateEmailTemplate("<template>", "<subject>") //Required

  resp, _, err := client.EmailTemplates.UpdateEmailTemplate(ctx, templateType).UpdateEmailTemplate(*updateEmailTemplate).Execute()
  if err != nil {
    var lrErr *Error
    if errors.As(err, &lrErr) {
      fmt.Println(lrErr.Description)
    }
    return
  }
  fmt.Println(resp)
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `templateType` | path | string | yes | The type of Email template to delete. |

### Request body

`UpdateEmailTemplate` as `application/json`.

### Returns

`EmailTemplateResponse`

